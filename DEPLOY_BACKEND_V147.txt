SHULE AI V147 RECOVERY — BACKEND

Render service settings when this backend ZIP is the repository root:
Build Command: npm ci --omit=dev
Pre-Deploy Command: npm run migrate
Start Command: npm start

New migration:
20260609000000-v147-approved-theme-media-recovery.js

Do not run npm start manually inside a live Render shell because Render already owns PORT 10000.
