Shule AI v145 BACKEND ONLY

Render settings:
Root Directory: leave blank if this backend ZIP is uploaded as the backend repo root.
Build Command: npm ci --omit=dev
Pre-Deploy Command: npm run migrate
Start Command: npm start

If your repository has a backend folder, put these files inside that backend folder and set:
Root Directory: backend
Build Command: npm ci --omit=dev
Pre-Deploy Command: npm run migrate
Start Command: npm start
