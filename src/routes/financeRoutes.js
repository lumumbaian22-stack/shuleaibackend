const r=require('express').Router(),{protect,authorize}=require('../middleware/auth'),c=require('../controllers/financeController');r.use(protect,authorize('admin','finance_officer'));r.get('/overview',c.requirePermission('overview'),c.getOverview);r.get('/alerts',c.requirePermission('alerts'),c.getAlerts);r.get('/expenses',c.requirePermission('expenses'),c.listExpenses);r.post('/expenses',c.requirePermission('expenses'),c.createExpense);r.patch('/expenses/:id',c.requirePermission('expenses'),c.updateExpense);r.delete('/expenses/:id',c.requirePermission('expenses'),c.deleteExpense);r.get('/report',c.requirePermission('reports'),c.getReport);module.exports=r;
