const{MediaAsset}=require('../models');exports.getAsset=async(req,res)=>{try{const asset=await MediaAsset.findOne({where:{token:req.params.token,isActive:true},skipTenantScope:true});if(!asset)return res.status(404).send('Media not found');const etag=`"${asset.checksum}"`;if(req.headers['if-none-match']===etag)return res.status(304).end();res.setHeader('Content-Type',asset.mimeType);res.setHeader('Content-Length',String(asset.byteSize||Buffer.byteLength(asset.data)));res.setHeader('Cache-Control','public, max-age=31536000, immutable');res.setHeader('ETag',etag);res.setHeader('Cross-Origin-Resource-Policy','cross-origin');return res.end(Buffer.from(asset.data));}catch(error){console.error('Media read error:',error);return res.status(500).send('Media unavailable');}};
