const DEFAULT=['overview','fee_structures','invoices','payments','verification','balances','defaulters','receipts','bursaries','expenses','reconciliation','reports','settings','alerts'];module.exports=function(permission,{adminAllowed=true}={}){return(req,res,next)=>{if(req.user?.role==='super_admin')return next();if(adminAllowed&&req.user?.role==='admin')return next();if(req.user?.role!=='finance_officer')return res.status(403).json({success:false,message:'Finance staff access required.'});const c=req.user.preferences?.finance?.permissions;const allowed=Array.isArray(c)&&c.length?c:DEFAULT;if(!allowed.includes(permission))return res.status(403).json({success:false,code:'FINANCE_PERMISSION_REQUIRED',message:`Finance permission required: ${permission}`});next();};};
